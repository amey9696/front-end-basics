// use const for creating array
const fruits = ['Apple', 'mango'];
fruits.push('banana'); //we do this because array can store in heap memory.
// fruits = ['chiku', 'grapes']; //we connot do this (if you use let then its possible )
console.log(fruits);