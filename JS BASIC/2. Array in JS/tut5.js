// for loop in array

let fruits = ["mango", 'grapes', 'banana', 'apple'];

console.log(fruits.length);
console.log("last element is:", fruits[fruits.length - 1]); //last element

for (let i = 0; i < fruits.length; i++) {
    console.log(fruits[i].toUpperCase());
}
